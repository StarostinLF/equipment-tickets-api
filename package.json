{
  "name": "equipment-tickets-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "REST API для учёта оборудования и заявок на его обслуживание",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "pino-pretty": "^13.0.0"
  }
}
